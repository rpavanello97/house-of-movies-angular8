export interface Movie {
    id?: number,
    title: string,
    urlPhoto?: string,
    releaseDate: Date,
    description?: string,
    imbdRate? : number,
    urlIMDb: string,
    gender: string,
}
